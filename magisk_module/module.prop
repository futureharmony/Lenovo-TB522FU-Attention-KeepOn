id=tb522fu_attention_keepon
name=Lenovo Y900 注视不熄屏增强 (WebUI)
version=v1.8.2
versionCode=182
author=futureharmony
description=为联想拯救者 Y900 (TB522FU) ColorOS 移植版启用原生硬件注视不熄屏。Camera 3 (OG0VE) AON FDPRO 硬件感知 + Demand Mode 按需判定（无界面后台 APK 承载 AttentionService 契约）。所有控制/测试/日志经管理器 WebUI 操作。含开机失败自动回退：连续 3 次开机失败自动禁用模块并由 service.d 脚本还原 secure 设置，可随时重新启用。v1.8.2 修正 self-test 的假阴性（判据改为容忍 daemon 重写 aon_evt、窗口 20s 且零帧重试一次、按「是否真在出 EVT」判定流状态，不再依赖稀疏且滞后的 HBT）；v1.8.1 修复 supervisor 重拉 daemon 时 IPC 路径不一致导致的静默失效，is_island 修复范围扩展至全部 nms_* 模型，supervisor 增加单实例锁与重拉退避。
